<h1>Kittens</h1>

<ul>
  <% @kittens.each do |kitten| %>
    <li>
      <%= link_to kitten.name, kitten %>
    </li>
  <% end %>
</ul>

<div class="actions">
  <%= link_to 'Create kitten', new_kitten_path %>
</div>